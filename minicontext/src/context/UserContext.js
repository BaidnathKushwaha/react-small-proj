import { createContext } from 'react'

const UserContext = createContext()
//creates a global data channel. Think of it like:Global Storage Pipe

export default UserContext;
//This creates a global container.